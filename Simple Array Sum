#include <stdio.h>
#include <string.h>

int main()
{
    int n , i , sum = 0 ,a[1000];
    scanf("%d",&n);
    for(i=0;i<n;i++)
    scanf("%d",&a[i]);
    for(i=0;i<n;i++)
    sum = sum + a[i];
    printf("%d",sum);
}

